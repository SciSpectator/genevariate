# GeneVariate - Required Dependencies
# Install: pip install -r requirements.txt

# Core data science
numpy
pandas
scipy
matplotlib
seaborn
scikit-learn

# GEO database / bioinformatics
GEOparse
requests
psutil

# AI / LLM
ollama

# LLM-GEO-Label-Extractor pipeline (optional — install if you plan to run
# `genevariate-llm-extract` or `genevariate --llm-extract`).
# Phase 1c semantic curator (BioLORD-2023) + DSPy-compiled prompts:
# dspy-ai>=2.5
# sentence-transformers>=2.7
# torch>=1.11
# transformers>=4.21

# RNA-seq raw-count DE pipeline (optional — the "RNA-seq DE (raw counts)" tool):
# install via `pip install genevariate[rnaseq]` for pydeseq2 + anndata.
# pydeseq2>=0.4
# anndata>=0.9

# Quantile normalization (optional but recommended)
qnorm

# Image handling (app icon / logo in GUI)
Pillow

# Tkinter is part of Python stdlib but may need system package:
#   Ubuntu/Debian: sudo apt install python3-tk
#   Fedora: sudo dnf install python3-tkinter
